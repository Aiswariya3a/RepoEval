---
phase: 01-foundation-authentication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/next.config.js
  - frontend/tsconfig.json
  - frontend/app/layout.tsx
  - frontend/app/globals.css
  - frontend/app/page.tsx
  - frontend/app/sign-in/page.tsx
  - frontend/app/dashboard/layout.tsx
  - frontend/app/dashboard/page.tsx
  - frontend/components/ui/button.tsx
  - frontend/components/ui/card.tsx
  - frontend/components/ui/avatar.tsx
  - frontend/components/dashboard/sidebar.tsx
  - frontend/components/dashboard/welcome-empty-state.tsx
  - frontend/lib/utils.ts
  - frontend/public/github-mark.svg
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "Next.js 16 development server starts and serves the application"
    - "shadcn/ui is initialized with Neutral/Stone palette and Inter font"
    - "Sign-in page displays centered card with 'RepoEval' branding, tagline, and 'Sign in with GitHub' button"
    - "Dashboard shell has sidebar (256px) with user avatar area, nav items placeholder, and 'Sign out' link"
    - "Dashboard content area shows welcome empty state with heading and 'Create Project' button"
    - "Sign-in page handles OAuth error display from URL query parameters"
    - "Root route '/' serves sign-in page (unauthenticated) or redirects to dashboard (authenticated)"
    - "Button, card, and avatar shadcn components are available in components/ui/"
    - "GitHub logo SVG is available as public asset for OAuth button"
    - "Page title is 'RepoEval' in browser tab"
  artifacts:
    - path: "frontend/app/sign-in/page.tsx"
      provides: "Sign-in page with centered card, branding, GitHub OAuth button, error display"
      min_lines: 50
    - path: "frontend/app/dashboard/layout.tsx"
      provides: "Dashboard shell with sidebar layout (256px) + content area"
      min_lines: 30
    - path: "frontend/app/dashboard/page.tsx"
      provides: "Welcome empty state with heading, description, and 'Create Project' button"
      min_lines: 30
    - path: "frontend/components/dashboard/sidebar.tsx"
      provides: "Sidebar with user avatar area, nav items placeholder, sign-out link"
      min_lines: 60
    - path: "frontend/app/layout.tsx"
      provides: "Root layout with Inter font, globals.css, metadata for 'RepoEval'"
      min_lines: 20
    - path: "frontend/public/github-mark.svg"
      provides: "GitHub logo SVG for OAuth button"
  key_links:
    - from: "frontend/app/layout.tsx"
      to: "frontend/app/globals.css"
      via: "import"
    - from: "frontend/app/layout.tsx"
      to: "next/font/google"
      via: "Inter({ subsets: ['latin'] })"
    - from: "frontend/app/sign-in/page.tsx"
      to: "frontend/components/ui/button.tsx"
      via: "import { Button }"
    - from: "frontend/app/sign-in/page.tsx"
      to: "frontend/components/ui/card.tsx"
      via: "import { Card, CardContent }"
    - from: "frontend/app/dashboard/layout.tsx"
      to: "frontend/components/dashboard/sidebar.tsx"
      via: "import { Sidebar }"
---

<objective>
Create the Next.js 16 frontend application with shadcn/ui, the sign-in page, and dashboard shell layout.

Purpose: Establish the frontend Foundation that all subsequent phases build upon. Deliver the complete auth UI (sign-in page with GitHub OAuth button + error states) and the dashboard shell layout that Phase 2+ will populate with project management features.

Output: A working Next.js 16 App Router frontend with shadcn/ui (Neutral/Stone, Inter font), sign-in page per UI-SPEC.md, and dashboard shell with sidebar.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md

<interfaces>
From UI-SPEC.md (locked design contract):

**Color tokens (CSS variables from shadcn Neutral/Stone):**
- `--background`: hsl(0 0% 100%) | `--muted`: hsl(0 0% 96.1%)
- `--foreground`: hsl(0 0% 3.9%) | `--muted-foreground`: hsl(0 0% 45.1%)
- `--card`: hsl(0 0% 100%) | `--card-foreground`: hsl(0 0% 3.9%)
- `--border`: hsl(0 0% 89.8%) | `--primary`: hsl(0 0% 9%)
- `--primary-foreground`: hsl(0 0% 100%) | `--accent`: hsl(0 0% 96.1%)

**Font:** Inter (variable, via next/font/google, subsets: ['latin'])
**Spacing:** Multiples of 4 (xs:4, sm:8, md:16, lg:24, xl:32, 2xl:48, 3xl:64)
**Exception:** GitHub button min-height: 44px (WCAG touch target)

**Copy (exact):**
- Title: "RepoEval" | Tagline: "AI-powered project evaluation reports in minutes"
- Button: "Sign in with GitHub" | Loading: "Redirecting to GitHub…"
- Dashboard heading: "Welcome to RepoEval" | Body: "Evaluate your first repository to get started."
- CTA (dashboard): "Create Project" | Sign out: "Sign out"
- OAuth errors: "Unable to sign in with GitHub. Please try again." / "No GitHub account found. Please sign in to a valid GitHub account." / "Too many attempts. Please wait a moment and try again."

**OAuth callback flow (from CONTEXT.md D-05):**
- OAuth callback served by FastAPI backend (not frontend)
- After successful auth, backend redirects to frontend /dashboard
- On error, backend redirects to /sign-in?error=&lt;code&gt;
- Frontend reads error query param and displays corresponding message

**Sign-in page layout:**
- Full viewport centered (min-h-screen, flex, items-center, justify-center)
- Card: w-full max-w-md (448px), p-8, border, shadow-lg, rounded-xl
- Logo centered, mb-8 | Tagline centered, mb-8, 28px semibold
- Button: full width (w-full), h-11 (44px), GitHub logo (24x24 SVG, mr-3)
- Error area: mt-4 below button, text-sm, rounded-md, p-3

**Dashboard shell layout:**
- Flex row: sidebar (w-64, 256px) + content (flex-1, p-8)
- Sidebar: p-4, border-r, full viewport height
- Top: avatar (h-8 w-8, 32px) + display name, mb-6
- Middle: nav items (flex-1, space-y-1) — Dashboard active for Phase 1
- Bottom: "Sign out" link, text-sm
- Content: centered empty state with heading + body + CTA button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: create Next.js 16 project, initialize shadcn/ui, and add button/card/avatar components</name>
  <files>
    frontend/package.json
    frontend/tsconfig.json
    frontend/next.config.js
    frontend/app/layout.tsx
    frontend/app/globals.css
    frontend/lib/utils.ts
    frontend/components/ui/button.tsx
    frontend/components/ui/card.tsx
    frontend/components/ui/avatar.tsx
  </files>
  <read_first>
    01-UI-SPEC.md (full design contract — palette, font, spacing, components)
  </read_first>
  <action>
    Steps to execute in order:

    1. Create the Next.js 16 project with TypeScript + Tailwind CSS v4 + App Router:
    ```bash
    npx create-next-app@latest frontend --typescript --tailwind --app --eslint --src-dir=false --import-alias="@/*" --use-npm --no-turbopack
    ```
    The `--no-turbopack` flag ensures webpack-based build for broader compatibility.
    If `create-next-app` prompts for any options, answer: Yes to TypeScript, Yes to Tailwind CSS, Yes to App Router, No to src/ directory, Yes to import alias @/*.
    If `create-next-app` is interactive, handle it by reading prompts and answering via keyboard input.

    2. Verify the project was created:
    ```bash
    if (Test-Path "frontend/package.json") { Write-Output "PASS" }
    ```

    3. Install shadcn/ui dependencies:
    ```bash
    cd frontend
    npx shadcn@latest init --defaults
    ```
    This initializes with Neutral/Stone color (default), CSS variables mode (default), and creates:
    - `components.json` with correct metadata
    - `app/globals.css` with CSS variable definitions
    - `lib/utils.ts` with cn() helper
    - Tailwind config updates for CSS variables

    4. Verify shadcn init succeeded:
    ```bash
    if (Test-Path "frontend/lib/utils.ts") { Write-Output "PASS: utils.ts exists" }
    $utils = Get-Content "frontend/lib/utils.ts" -Raw; if ($utils -match "cn") { Write-Output "PASS: cn() helper exists" }
    ```

    5. Add required shadcn components:
    ```bash
    cd frontend
    npx shadcn@latest add button card avatar
    ```

    6. Verify components were added:
    ```bash
    if (Test-Path "frontend/components/ui/button.tsx") { Write-Output "PASS: button.tsx" }
    if (Test-Path "frontend/components/ui/card.tsx") { Write-Output "PASS: card.tsx" }
    if (Test-Path "frontend/components/ui/avatar.tsx") { Write-Output "PASS: avatar.tsx" }
    ```

    7. Update `frontend/app/layout.tsx` to use Inter font with correct metadata:
    ```tsx
    import type { Metadata } from "next";
    import { Inter } from "next/font/google";
    import "./globals.css";

    const inter = Inter({
      subsets: ["latin"],
      display: "swap",
    });

    export const metadata: Metadata = {
      title: "RepoEval",
      description: "AI-powered project evaluation reports in minutes",
    };

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html lang="en">
          <body className={inter.className}>{children}</body>
        </html>
      );
    }
    ```

    8. Update `frontend/next.config.js` to add API rewrites for proxying backend calls during development:
    ```javascript
    /** @type {import('next').NextConfig} */
    const nextConfig = {
      async rewrites() {
        return [
          {
            source: "/api/:path*",
            destination: `${process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000"}/api/:path*`,
          },
        ];
      },
    };

    module.exports = nextConfig;
    ```

    Use the write tool for file modifications. Do NOT use heredocs.
  </action>
  <verify>
    <automated>
      if (Test-Path "frontend/package.json") { Write-Output "PASS: Next.js project created" }
      if (Test-Path "frontend/components/ui/button.tsx") { Write-Output "PASS: shadcn button" }
      if (Test-Path "frontend/components/ui/card.tsx") { Write-Output "PASS: shadcn card" }
      if (Test-Path "frontend/components/ui/avatar.tsx") { Write-Output "PASS: shadcn avatar" }
      if (Test-Path "frontend/lib/utils.ts") { Write-Output "PASS: lib/utils.ts" }
      $layout = Get-Content "frontend/app/layout.tsx" -Raw; if ($layout -match "RepoEval") { Write-Output "PASS: metadata title set" }
      if ($layout -match "Inter") { Write-Output "PASS: Inter font loaded" }
    </automated>
  </verify>
  <acceptance_criteria>
    - Next.js 16 project created in frontend/ with TypeScript, Tailwind CSS, App Router
    - shadcn/ui initialized with default config (Neutral/Stone, CSS variables)
    - button.tsx, card.tsx, avatar.tsx exist in frontend/components/ui/
    - lib/utils.ts has cn() function
    - Root layout uses Inter font with display=swap and subsets=['latin']
    - Metadata title is "RepoEval"
    - next.config.js has API rewrite proxy to backend
  </acceptance_criteria>
  <done>
    Next.js 16 frontend project initialized with shadcn/ui (Neutral/Stone, Inter). Button, card, avatar components available. API rewrites configured for backend proxy.
  </done>
</task>

<task type="auto">
  <name>task 2: build sign-in page with centered card, branding, GitHub OAuth button, and inline error handling</name>
  <files>
    frontend/public/github-mark.svg
    frontend/app/sign-in/page.tsx
    frontend/app/page.tsx
  </files>
  <read_first>
    frontend/app/layout.tsx (task 1 output — understand root layout)
    frontend/components/ui/button.tsx (task 1 output — understand Button props)
    frontend/components/ui/card.tsx (task 1 output — understand Card components)
    UI-SPEC.md §Interactions / §Pre-Auth Sign-In Page (exact layout, copy, error codes)
  </read_first>
  <action>
    Create three files:

    1. `frontend/public/github-mark.svg` — GitHub Mark logo (the simple Octocat icon, not the wordmark):
    ```svg
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M12 0C5.37 0 0 5.37 0 12C0 17.31 3.435 21.795 8.205 23.385C8.805 23.49 9.03 23.13 9.03 22.815C9.03 22.53 9.015 21.585 9.015 20.58C6 21.135 5.22 19.845 4.98 19.17C4.845 18.825 4.26 17.76 3.75 17.475C3.33 17.25 2.73 16.695 3.735 16.68C4.68 16.665 5.355 17.55 5.58 17.91C6.66 19.725 8.385 19.215 9.075 18.9C9.18 18.12 9.495 17.595 9.84 17.295C7.17 16.995 4.38 15.96 4.38 11.37C4.38 10.065 4.845 8.985 5.61 8.145C5.49 7.845 5.07 6.615 5.73 4.965C5.73 4.965 6.735 4.65 9.03 6.195C9.99 5.925 11.01 5.79 12.03 5.79C13.05 5.79 14.07 5.925 15.03 6.195C17.325 4.635 18.33 4.965 18.33 4.965C18.99 6.615 18.57 7.845 18.45 8.145C19.215 8.985 19.68 10.05 19.68 11.37C19.68 15.975 16.875 16.995 14.205 17.295C14.64 17.67 15.015 18.39 15.015 19.515C15.015 21.12 15 22.41 15 22.815C15 23.13 15.225 23.505 15.825 23.385C20.565 21.795 24 17.31 24 12C24 5.37 18.63 0 12 0Z" fill="currentColor"/>
    </svg>
    ```

    2. `frontend/app/sign-in/page.tsx` — full sign-in page with error handling:
    ```tsx
    "use client";

    import { useSearchParams } from "next/navigation";
    import { Suspense } from "react";
    import { Button } from "@/components/ui/button";
    import {
      Card,
      CardContent,
    } from "@/components/ui/card";
    import Image from "next/image";

    const OAUTH_ERRORS: Record<string, string> = {
      generic: "Unable to sign in with GitHub. Please try again.",
      no_account: "No GitHub account found. Please sign in to a valid GitHub account.",
      rate_limited: "Too many attempts. Please wait a moment and try again.",
      expired: "Your session has expired. Please sign in again.",
    };

    function SignInContent() {
      const searchParams = useSearchParams();
      const errorCode = searchParams.get("error");
      const errorMessage = errorCode ? OAUTH_ERRORS[errorCode] || OAUTH_ERRORS.generic : null;

      return (
        <main className="min-h-screen flex items-center justify-center bg-background p-4">
          <Card className="w-full max-w-md shadow-lg">
            <CardContent className="p-8 flex flex-col items-center">
              <div className="mb-8 text-center">
                <h1 className="text-2xl font-semibold tracking-tight">RepoEval</h1>
              </div>
              <p className="text-[28px] font-semibold leading-tight text-center mb-8">
                AI-powered project evaluation reports in minutes
              </p>
              <Button
                className="w-full h-11"
                onClick={() => {
                  window.location.href = `${process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000"}/api/auth/login`;
                }}
              >
                <Image
                  src="/github-mark.svg"
                  alt="GitHub"
                  width={24}
                  height={24}
                  className="mr-3"
                />
                Sign in with GitHub
              </Button>
              {errorMessage && (
                <div className="mt-4 w-full text-sm text-destructive bg-destructive/10 rounded-md p-3 text-center">
                  {errorMessage}
                </div>
              )}
            </CardContent>
          </Card>
        </main>
      );
    }

    export default function SignInPage() {
      return (
        <Suspense>
          <SignInContent />
        </Suspense>
      );
    }
    ```

    3. `frontend/app/page.tsx` — root route that renders sign-in page (auth check will be added in Plan 04):
    ```tsx
    import SignInPage from "./sign-in/page";

    export default function HomePage() {
      return <SignInPage />;
    }
    ```

    Use the write tool for each file. Do NOT use heredocs.
  </action>
  <verify>
    <automated>
      if (Test-Path "frontend/app/sign-in/page.tsx") { Write-Output "PASS: sign-in page exists" }
      if (Test-Path "frontend/app/page.tsx") { Write-Output "PASS: root page exists" }
      if (Test-Path "frontend/public/github-mark.svg") { Write-Output "PASS: GitHub logo exists" }
      $content = Get-Content "frontend/app/sign-in/page.tsx" -Raw
      if ($content -match "useSearchParams") { Write-Output "PASS: uses search params for errors" }
      if ($content -match "/api/auth/login") { Write-Output "PASS: links to backend OAuth login" }
      if ($content -match "OAUTH_ERRORS") { Write-Output "PASS: error mapping defined" }
    </automated>
  </verify>
  <acceptance_criteria>
    Sign-in page at /sign-in renders: centered card (max-w-md), "RepoEval" heading, tagline (28px semibold), "Sign in with GitHub" button with GitHub logo, inline error area hidden on load but rendered when ?error= param present
    Root route / renders sign-in page content
    GitHub logo SVG is served from /github-mark.svg
  </acceptance_criteria>
  <done>
    Sign-in page implemented per UI-SPEC with correct copy, layout (centered card max-w-md, border, shadow-lg), GitHub OAuth button (44px height, w-full), and error handling for all four error codes (generic, no_account, rate_limited, expired).
  </done>
</task>

<task type="auto">
  <name>task 3: build dashboard shell layout with sidebar and welcome empty state</name>
  <files>
    frontend/app/dashboard/layout.tsx
    frontend/app/dashboard/page.tsx
    frontend/components/dashboard/sidebar.tsx
    frontend/components/dashboard/welcome-empty-state.tsx
  </files>
  <read_first>
    frontend/components/ui/avatar.tsx (task 1 — understand Avatar props)
    frontend/components/ui/button.tsx (task 1)
    UI-SPEC.md §Post-Auth Dashboard Shell (exact layout, sidebar width 256px, nav items, empty state)
    UI-SPEC.md §Layout Specs Dashboard Shell table (all spacing values)
  </read_first>
  <action>
    Create four files:

    1. `frontend/components/dashboard/sidebar.tsx`:
    ```tsx
    import Link from "next/link";
    import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";

    // Phase 1: Static user data. Plan 04 will wire real auth data.
    const MOCK_USER = {
      display_name: "User",
      avatar_url: null,
    };

    export function Sidebar() {
      const initials = MOCK_USER.display_name
        .split(" ")
        .map((n) => n[0])
        .join("")
        .toUpperCase()
        .slice(0, 2);

      return (
        <aside className="w-64 h-screen border-r border-border flex flex-col p-4 bg-card">
          {/* User area */}
          <div className="flex items-center gap-3 mb-6">
            <Avatar className="h-8 w-8">
              <AvatarImage src={MOCK_USER.avatar_url || undefined} alt={MOCK_USER.display_name} />
              <AvatarFallback className="text-xs">{initials}</AvatarFallback>
            </Avatar>
            <span className="text-sm font-medium truncate">{MOCK_USER.display_name}</span>
          </div>

          {/* Navigation */}
          <nav className="flex-1 space-y-1">
            <Link
              href="/dashboard"
              className="w-full flex items-center gap-3 px-3 py-2 text-sm rounded-md bg-accent text-accent-foreground font-medium"
            >
              Dashboard
            </Link>
            <span className="w-full flex items-center gap-3 px-3 py-2 text-sm rounded-md text-muted-foreground cursor-not-allowed">
              Projects
            </span>
            <span className="w-full flex items-center gap-3 px-3 py-2 text-sm rounded-md text-muted-foreground cursor-not-allowed">
              Settings
            </span>
          </nav>

          {/* Sign out */}
          <Link
            href="/sign-in"
            className="px-3 py-2 text-sm text-muted-foreground hover:underline"
          >
            Sign out
          </Link>
        </aside>
      );
    }
    ```

    2. `frontend/components/dashboard/welcome-empty-state.tsx`:
    ```tsx
    import { Button } from "@/components/ui/button";

    export function WelcomeEmptyState() {
      return (
        <div className="flex flex-col items-center justify-center h-full text-center max-w-prose mx-auto">
          <h2 className="text-xl font-semibold mb-2">Welcome to RepoEval</h2>
          <p className="text-muted-foreground mb-6">
            Evaluate your first repository to get started.
          </p>
          <Button size="default">
            Create Project
          </Button>
        </div>
      );
    }
    ```

    3. `frontend/app/dashboard/layout.tsx`:
    ```tsx
    import { Sidebar } from "@/components/dashboard/sidebar";

    export default function DashboardLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <div className="flex h-screen">
          <Sidebar />
          <main className="flex-1 p-8 overflow-auto">
            {children}
          </main>
        </div>
      );
    }
    ```

    4. `frontend/app/dashboard/page.tsx`:
    ```tsx
    import { WelcomeEmptyState } from "@/components/dashboard/welcome-empty-state";

    export default function DashboardPage() {
      return <WelcomeEmptyState />;
    }
    ```

    Use the write tool for each file.
  </action>
  <verify>
    <automated>
      if (Test-Path "frontend/app/dashboard/layout.tsx") { Write-Output "PASS: dashboard layout" }
      if (Test-Path "frontend/app/dashboard/page.tsx") { Write-Output "PASS: dashboard page" }
      if (Test-Path "frontend/components/dashboard/sidebar.tsx") { Write-Output "PASS: sidebar component" }
      if (Test-Path "frontend/components/dashboard/welcome-empty-state.tsx") { Write-Output "PASS: welcome component" }
      $sidebar = Get-Content "frontend/components/dashboard/sidebar.tsx" -Raw
      if ($sidebar -match "w-64") { Write-Output "PASS: sidebar width 256px" }
      if ($sidebar -match "Sign out") { Write-Output "PASS: sign out link" }
      if ($sidebar -match "Dashboard") { Write-Output "PASS: nav has Dashboard" }
      $welcome = Get-Content "frontend/components/dashboard/welcome-empty-state.tsx" -Raw
      if ($welcome -match "Welcome to RepoEval") { Write-Output "PASS: welcome heading" }
      if ($welcome -match "Create Project") { Write-Output "PASS: create project button" }
    </automated>
  </verify>
  <acceptance_criteria>
    - Dashboard layout renders sidebar (w-64, full viewport height, border-r, p-4, bg-card) + content area (flex-1, p-8)
    - Sidebar has: user avatar (h-8 w-8) + display name at top, "Dashboard" active nav item, "Projects" placeholder, "Settings" placeholder, "Sign out" link at bottom
    - Dashboard content shows centered welcome empty state with h2 "Welcome to RepoEval", p "Evaluate your first repository to get started.", "Create Project" button
    - All spacings match UI spec (sidebar p-4, content p-8, avatar h-8 w-8, nav items px-3 py-2)
  </acceptance_criteria>
  <done>
    Dashboard shell layout complete with sidebar (256px, avatar, nav, sign out) and welcome empty state. Structure ready for Phase 2 project management features.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→nextjs | Browser to Vercel/Next.js. Public assets served without authentication. |
| nextjs→backend | Next.js rewrites proxy /api/* to FastAPI backend. Cookies forwarded via credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | Public assets | accept | github-mark.svg is a public GitHub logo. No sensitive data. |
| T-01-07 | Spoofing | Sign-in page | mitigate | "Sign in with GitHub" button redirects to backend /api/auth/login — users will always see github.com domain in browser before authorizing. No frontend OAuth token handling. |
| T-01-08 | Information Disclosure | Error messages | mitigate | Error codes are generic (no_account, rate_limited, expired, generic). No sensitive info leaked in URL params. |

</threat_model>

<verification>

**Plan-level verification:**
1. ✅ `npm run dev` starts the Next.js dev server without errors
2. ✅ shadcn/ui components (button, card, avatar) render without import errors
3. ✅ Sign-in page URL `/sign-in` renders centered card with branding
4. ✅ Dashboard URL `/dashboard` renders sidebar + welcome state
5. ✅ Root URL `/` renders sign-in page content
6. ✅ `?error=no_account` query param displays correct error message

</verification>

<success_criteria>
1. Next.js 16 dev server starts on `localhost:3000` without errors
2. `/sign-in` page displays per UI-SPEC (centered card, "RepoEval", tagline, GitHub button, error area)
3. `/dashboard` page displays sidebar (256px, avatar, nav, sign out) + welcome empty state
4. `?error=no_account` shows inline error "No GitHub account found..."
5. `?error=expired` shows inline error "Your session has expired..."
6. All three shadcn components (button, card, avatar) render correctly
7. Inter font loads via next/font/google and applies globally

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-02-SUMMARY.md`
</output>
