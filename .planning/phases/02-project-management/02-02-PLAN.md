---
phase: 02-project-management
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/lib/api-projects.ts
  - frontend/components/dashboard/project-card.tsx
  - frontend/components/dashboard/skeleton-card.tsx
  - frontend/components/dashboard/project-grid.tsx
  - frontend/app/projects/page.tsx
  - frontend/app/projects/layout.tsx
  - frontend/app/projects/new/page.tsx
  - frontend/app/dashboard/page.tsx
  - frontend/components/dashboard/sidebar.tsx
  - frontend/components/dashboard/welcome-empty-state.tsx
autonomous: true
requirements:
  - AUTH-02
user_setup: []

must_haves:
  truths:
    - "Authenticated user sees project cards on /dashboard when projects exist"
    - "Authenticated user sees skeleton cards while projects load"
    - "User sees empty state with Create CTA on /dashboard when no projects exist"
    - "Sidebar Projects nav item links to /projects route"
    - "User can navigate to /projects/new and see a create form with name, description, tags"
    - "User can create a project and is redirected to its detail page"
    - "Each project card shows: name, description preview, created date, repo count (0), three-dot menu"
    - "Cards display in CSS grid auto-fill minmax(300px, 1fr)"
    - "Pagination controls appear when more than 12 projects exist"
  artifacts:
    - path: "frontend/lib/api-projects.ts"
      provides: "API client for all project CRUD operations"
      exports: ["Project", "ProjectCreate", "ProjectUpdate", "PaginatedResponse", "listProjects", "createProject", "getProject", "updateProject", "deleteProject"]
    - path: "frontend/app/projects/page.tsx"
      provides: "Dedicated projects list page at /projects route"
      uses: "ProjectGrid component"
    - path: "frontend/app/projects/new/page.tsx"
      provides: "Project creation form at /projects/new"
      renders: "Form with name, description, tags, repo URL inputs"
    - path: "frontend/components/dashboard/project-card.tsx"
      provides: "Individual project card with hover lift + three-dot menu + footer"
    - path: "frontend/components/dashboard/project-grid.tsx"
      provides: "CSS grid of project cards with skeleton loading and pagination"
    - path: "frontend/components/dashboard/skeleton-card.tsx"
      provides: "Skeleton loading placeholder card"
  key_links:
    - from: "frontend/app/dashboard/page.tsx"
      to: "frontend/components/dashboard/project-grid.tsx"
      via: "Import"
    - from: "frontend/app/projects/page.tsx"
      to: "frontend/components/dashboard/project-grid.tsx"
      via: "Import (shared component)"
    - from: "frontend/components/dashboard/project-grid.tsx"
      to: "frontend/lib/api-projects.ts"
      via: "listProjects API call"
    - from: "frontend/components/dashboard/sidebar.tsx"
      to: "/projects"
      via: "Link href"

---

<objective>
Build the frontend project list view with card grid, empty state, skeleton loading, and create project page.

**Purpose:** Users can see their projects as cards on the dashboard (or /projects route) with loading skeletons, pagination, and create new projects through a dedicated form. This is the main entry point for project management.

**Output:** Shared ProjectGrid component, project cards with three-dot menus, skeleton loading, dashboard integration, /projects route, /projects/new create page, sidebar nav update, welcome empty state update.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-project-management/02-CONTEXT.md (D-01 through D-12, D-14, D-16, D-17, D-18)

@frontend/app/auth-provider.tsx (useAuth hook)
@frontend/app/dashboard/page.tsx (current — renders WelcomeEmptyState)
@frontend/app/dashboard/layout.tsx (protected layout with sidebar + main)
@frontend/components/dashboard/sidebar.tsx (sidebar with disabled Projects link)
@frontend/components/dashboard/welcome-empty-state.tsx (current empty state)
@frontend/lib/auth.ts (fetchApi pattern)
@frontend/components/ui/card.tsx (shadcn Card: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, CardAction)
@frontend/components/ui/button.tsx (shadcn Button)

<interfaces>
From frontend/lib/auth.ts:
```typescript
export async function fetchApi<T>(path: string, options?: RequestInit): Promise<T>
// Uses credentials: "include", Content-Type: application/json, returns parsed JSON
```

From frontend/components/ui/card.tsx:
```tsx
Card, CardHeader, CardTitle, CardDescription, CardAction, CardContent, CardFooter
// All use data-slot attributes, can use group/card for hover effects
```

From frontend/app/dashboard/layout.tsx:
```tsx
// Protected: redirects to /sign-in if !user
// Renders: <div class="flex h-screen"><Sidebar /><main>{children}</main></div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Create project types and API client module</name>
  <files>frontend/lib/api-projects.ts</files>

  <read_first>frontend/lib/auth.ts</read_first>

  <action>
    Create `frontend/lib/api-projects.ts` with:

    1. **Types:**
       - `Project { id: string; name: string; description: string | null; tags: string[] | null; created_at: string; updated_at: string; }`
       - `ProjectCreate { name: string; description?: string; tags?: string[]; }` (no repo_urls — deferred to Phase 3 per D-30)
       - `ProjectUpdate { name?: string; description?: string; tags?: string[]; }`
       - `PaginatedResponse<T> { items: T[]; total: number; page: number; page_size: number; total_pages: number; }`

    2. **API functions** (all use `fetchApi` from `@/lib/auth`):
       - `listProjects(page=1, pageSize=12): Promise<PaginatedResponse<Project>>` — GET /api/projects?page=&page_size=
       - `createProject(data: ProjectCreate): Promise<Project>` — POST /api/projects with JSON body
       - `getProject(id: string): Promise<Project>` — GET /api/projects/{id}
       - `updateProject(id: string, data: ProjectUpdate): Promise<Project>` — PATCH /api/projects/{id} with JSON body
       - `deleteProject(id: string): Promise<void>` — DELETE /api/projects/{id}, ignores response body

    Import `fetchApi` from `@/lib/auth` using the same pattern. All functions use `credentials: "include"` via fetchApi defaults.
  </action>

  <verify>
    <automated>
      cd frontend; if (Test-Path lib/api-projects.ts) { Write-Output "api-projects.ts created"; grep -c "export" lib/api-projects.ts }
    </automated>
  </verify>

  <acceptance_criteria>
    - All 4 type interfaces and 5 API functions are exported
    - Each API function delegates to fetchApi with correct HTTP method and path
    - createProject does NOT include repo_urls field (deferred per D-30)
    - listProjects accepts page/page_size query params
  </acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: Create project card, skeleton card, and grid components</name>
  <files>
    frontend/components/dashboard/project-card.tsx
    frontend/components/dashboard/skeleton-card.tsx
    frontend/components/dashboard/project-grid.tsx
  </files>

  <read_first>
    frontend/components/ui/card.tsx
    frontend/components/ui/button.tsx
  </read_first>

  <action>
    Create three components using existing shadcn patterns:

    **1. `project-card.tsx`** (per D-01, D-02, D-04, D-05):
    - Props: `{ project: Project }`
    - Wraps card in `<Link href={/projects/${project.id}}>` with hover lift: `transition-all duration-200 hover:-translate-y-0.5 hover:shadow-lg`
    - Header: CardTitle with project.name, CardAction slot with a ghost icon button (MoreHorizontal from lucide-react) — full dropdown menu wired in 02-03
    - Content: description with `line-clamp-2` class
    - Footer: Calendar icon + formatted created_at date, GitBranch icon + "0 repos" text (placeholder until Phase 3 per specifics)
    - Import Card, CardHeader, CardTitle, CardContent, CardFooter from @/components/ui/card
    - Import { Calendar, GitBranch, MoreHorizontal } from "lucide-react"

    **2. `skeleton-card.tsx`** (per D-07):
    - Renders Card with animate-pulse class
    - 3 muted placeholder divs: header bar (h-4 w-3/4), content bars (h-3 w-full + h-3 w-2/3 stacked), footer bar (h-3 w-1/3)
    - All use `bg-muted rounded` class

    **3. `project-grid.tsx`** (per D-03, D-06, D-08):
    - "use client" component with state: data (PaginatedResponse | null), loading (boolean), page (number)
    - Props: `{ emptyFallback?: React.ReactNode }` — rendered when response has 0 items
    - useEffect fetches `listProjects(page)` on mount and page change
    - Loading state: 6-column CSS grid `grid-cols-[repeat(auto-fill,minmax(300px,1fr))] gap-4` with SkeletonCard components
    - Empty state (data exists but items.length === 0): render emptyFallback if provided
    - Loaded state: same CSS grid with ProjectCard for each item
    - Pagination (when total_pages > 1): centered flex container with prev/next ChevronLeft/ChevronRight buttons (outline variant, disabled at edges) and page number buttons (default for current page, outline for others)
    - Import from lucide-react: ChevronLeft, ChevronRight
    - Import Button from @/components/ui/button
  </action>

  <verify>
    <automated>
      cd frontend; foreach ($f in @("project-card", "skeleton-card", "project-grid")) { if (Test-Path "components/dashboard/$f.tsx") { Write-Output "$f.tsx exists" } }
      cd frontend; grep -c "auto-fill,minmax" components/dashboard/project-grid.tsx
    </automated>
  </verify>

  <acceptance_criteria>
    - ProjectCard renders name, descriptions (2-line clamp), created date, repo count "0 repos", three-dot button
    - ProjectCard wraps in Link to /projects/[id] with hover lift effect
    - SkeletonCard renders 3 muted bars with pulse animation
    - ProjectGrid uses identical CSS grid in loading and loaded states
    - Pagination renders when total_pages > 1, hides when == 1
    - ProjectGrid accepts emptyFallback prop
  </acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: Wire dashboard, /projects route, /projects/new, sidebar, and empty state</name>
  <files>
    frontend/app/dashboard/page.tsx
    frontend/app/projects/layout.tsx
    frontend/app/projects/page.tsx
    frontend/app/projects/new/page.tsx
    frontend/components/dashboard/sidebar.tsx
    frontend/components/dashboard/welcome-empty-state.tsx
  </files>

  <read_first>
    frontend/app/dashboard/page.tsx
    frontend/app/dashboard/layout.tsx
    frontend/components/dashboard/sidebar.tsx
    frontend/components/dashboard/welcome-empty-state.tsx
  </read_first>

  <action>
    1. **Modify `frontend/components/dashboard/welcome-empty-state.tsx`** (per D-09):
       - Change "Create Project" button to use next/link `Link` component pointing to `/projects/new`
       - Wrap button in `<Link href="/projects/new">` or make button use Link's href via Next.js `useRouter`

    2. **Modify `frontend/components/dashboard/sidebar.tsx`** (per D-11):
       - Change the disabled "Projects" span to an active `<Link href="/projects">` with proper styling (same pattern as Dashboard link — `bg-accent text-accent-foreground font-medium` when on /projects path)
       - Use `usePathname()` from `next/navigation` to detect active route and apply highlight class
       - Use existing pattern from the Dashboard link: `className="w-full flex items-center gap-3 px-3 py-2 text-sm rounded-md bg-accent text-accent-foreground font-medium"` when active, otherwise `text-muted-foreground hover:bg-muted hover:text-foreground`

    3. **Create `frontend/app/projects/layout.tsx`**:
       - Re-export the dashboard layout (copy from frontend/app/dashboard/layout.tsx) to ensure /projects route has sidebar protection
       - OR import and re-export: `export { default } from "../dashboard/layout"`

    4. **Create `frontend/app/projects/page.tsx`** (per D-10):
       - Render `<ProjectGrid emptyFallback={<WelcomeEmptyState />} />`

    5. **Modify `frontend/app/dashboard/page.tsx`** (per D-09, D-10):
       - Replace direct WelcomeEmptyState with `<ProjectGrid emptyFallback={<WelcomeEmptyState />} />`
       - Import ProjectGrid from @/components/dashboard/project-grid

    6. **Create `frontend/app/projects/new/page.tsx`** (per D-14, D-16, D-17, D-18):
       - "use client" form component
       - **Inputs:**
         - Name: required text input with label, regex validation `^[a-zA-Z0-9 _-]+$`, 1-100 chars, show validation error inline
         - Description: optional textarea with label
         - Repo URLs: text input (decorative only per D-30 — "Repo URLs will be available for setup in a future update" tooltip or disabled state)
         - Tags: tag selector with predefined options (D-17 list) + custom tag input
       - Predefined tags list (hardcoded per D-17): Frontend, Backend, Full Stack, Mobile, AI/ML, Data Science, DevOps, Cloud, Open Source, Research, Hackathon, Capstone, Enterprise, Library, API, Microservices, CLI, Web Application
       - Tag selector: show available predefined tags as clickable badges; selected ones become active; custom tag input allows typing new tags
       - Form submission: call `createProject(formData)`, on success `router.push(/projects/${newProject.id})`, on error show error message
       - Cancel button navigates to `/projects` (per D-18)
       - Form validation: name required, min 1 char, max 100 chars, alphanumeric+spaces+dashes+underscores only

    Note: Use native HTML input/textarea elements with proper styling (className patterns from shadcn) rather than building full Input/Textarea components. The tags selector can use flexbox with button badges.
  </action>

  <verify>
    <automated>
      cd frontend; foreach ($f in @("app/projects/page.tsx", "app/projects/layout.tsx", "app/projects/new/page.tsx")) { if (Test-Path $f) { Write-Output "$f created" } }
      cd frontend; grep -c "ProjectGrid" app/dashboard/page.tsx
      cd frontend; grep -c "/projects" components/dashboard/sidebar.tsx
    </automated>
  </verify>

  <acceptance_criteria>
    - /dashboard shows ProjectGrid with WelcomeEmptyState fallback
    - /projects shows ProjectGrid with WelcomeEmptyState fallback (inside dashboard layout)
    - /projects/new shows form with name (required), description, repo URLs (disabled/decorative), tags selector
    - Tags selector shows 18 predefined tags from D-17 plus custom tag input
    - Form validates name (1-100 chars, alphanumeric + spaces/dashes/underscores), shows inline error
    - Successful create redirects to /projects/[id]
    - Cancel navigates to /projects
    - Sidebar Projects link navigates to /projects and highlights when active
    - WelcomeEmptyState Create button links to /projects/new
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Untrusted user input crosses from client to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Create form input | mitigate | Client-side validation (name regex, max length); backend also validates |
| T-02-07 | Information Disclosure | Project list display | mitigate | Backend filters by owner_id; frontend only shows what backend returns |
</threat_model>

<verification>
1. Dashboard shows skeleton cards on initial load, cards when projects exist, empty state when none
2. /projects route renders same content as dashboard (shared component)
3. Create form validates name, shows error, submits to POST /api/projects
4. After create, redirects to /projects/{newId}
5. Sidebar Projects link is active on /projects and /projects/new
6. All D-01 through D-12 and D-14, D-16, D-17, D-18 decisions implemented
</verification>

<success_criteria>
- Users can see project cards in CSS grid with skeleton loading and pagination
- Users can create projects via /projects/new form
- Dashboard swaps between empty state and project grid
- Sidebar navigation properly links to /projects
- All user-facing decisions from CONTEXT.md implemented
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-management/02-02-SUMMARY.md`
</output>
